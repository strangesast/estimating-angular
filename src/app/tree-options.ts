export interface TreeOptions {
  expand:  boolean; // expand on focus
  source:  boolean; // drag into
  sink:    boolean; // drag from
  reorder: boolean; // reorder drag
  sort:    boolean; // sortable
  animationTime: number; // transition time in ms
};
